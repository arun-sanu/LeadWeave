apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "leadweave.fullname" . }}
  labels:
    {{- include "leadweave.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
