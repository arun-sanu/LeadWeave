const { createClient } = require('@supabase/supabase-js');

const url = 'https://mlddsyylbkzlrqdhonfr.supabase.co';
const key = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1sZGRzeXlsYmt6bHJxZGhvbmZyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODc0NzEyNTgsImV4cCI6MjEwMzA0NzI1OH0.Rq5fpFEJYlK119FcbUDCkZTpIIa5EYUeDIME_-J6OBA';

const supabase = createClient(url, key);

(async () => {
  console.log('🔍 Testing login formats...');

  const combinations = [
    // LeadWeave pseudo-email format generated by frontend
    { email: 'arun@modbitlabs.auth.leadweave', password: 'Nightshade', company: 'ModBit Labs' },
    { email: 'arun@lifegrains.auth.leadweave', password: 'Nightshade', company: 'LifeGrains' },
    // Standard email format
    { email: 'arun@modbitlabs.com', password: 'Nightshade', company: 'ModBit Labs' },
    { email: 'arun@lifegrains.in', password: 'Nightshade', company: 'LifeGrains' },
  ];

  for (const c of combinations) {
    console.log(`\n🔑 Testing Email: "${c.email}" | Password: "${c.password}"`);
    const { data, error } = await supabase.auth.signInWithPassword({
      email: c.email,
      password: c.password
    });

    if (error) {
      console.log(` ❌ Failed: ${error.message}`);
    } else {
      console.log(` ✅ SUCCESS! User ID: ${data.user.id}`);
      console.log(`    User Metadata:`, JSON.stringify(data.user.user_metadata));
    }
  }
})();
